"use client";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Select } from "@/components/ui/primitives";
export function UserAccess({userId,initialRole,initialSuspended,locale}:{userId:string;initialRole:string;initialSuspended:boolean;locale:"fa"|"en"}){const [role,setRole]=useState(initialRole);const [suspended,setSuspended]=useState(initialSuspended);const [busy,setBusy]=useState(false);const [message,setMessage]=useState("");return <div className="mt-3 flex flex-wrap items-end gap-2"><label className="text-xs font-bold"><span className="mb-1 block">Role</span><Select value={role} onChange={(e)=>setRole(e.target.value)} className="h-9"><option value="superadmin">superadmin</option><option value="creator">creator</option><option value="admin">admin</option><option value="finance">finance</option><option value="support">support</option><option value="editor">editor</option><option value="business">business</option><option value="consumer">consumer</option></Select></label><label className="flex h-9 items-center gap-2 rounded-xl border border-line px-3 text-xs font-bold"><input type="checkbox" checked={suspended} onChange={(e)=>setSuspended(e.target.checked)}/>{locale==="fa"?"تعلیق":"Suspended"}</label><Button size="sm" variant="outline" loading={busy} onClick={async()=>{setBusy(true);setMessage("");const res=await fetch("/api/admin/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({userId,role,isSuspended:suspended})});setMessage(res.ok?(locale==="fa"?"ذخیره شد":"Saved"):(locale==="fa"?"ذخیره نشد":"Failed"));setBusy(false)}}>{locale==="fa"?"ذخیره دسترسی":"Save access"}</Button>{message?<span className="text-xs text-muted" role="status">{message}</span>:null}</div>}
